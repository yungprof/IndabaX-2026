[pytest]
markers =
    unit: Unit tests — no API key required, tools tested in isolation
    integration: Integration tests — requires OPENROUTER_API_KEY
    advanced: Advanced/optional tests — for extended exercises
testpaths = tests
